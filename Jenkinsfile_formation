pipeline {
    agent {
        docker {
            image 'openjdk:8-jdk-alpine'
            args '-p 8080:8080' 
        } 
    }
    stage('Clean') {
        steps {
            sh 'mvn clean'
        }
    }
    stage('Build') {
        steps {
            sh 'mvn build'
        }
    }
}